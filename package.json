{
  "name": "vite-plugin-tauri",
  "version": "0.1.11",
  "description": "Integrate Tauri in a Vite project to build cross-platform apps.",
  "repository": "https://github.com/amrbashir/vite-plugin-tauri.git",
  "author": "Amr Bashir <amr.bashir2015@gmail.com>",
  "license": "MIT",
  "files": [
    "bin"
  ],
  "bin": {
    "vite-tauri": "bin/index.js"
  },
  "type": "module",
  "scripts": {
    "prepublishOnly": "pnpm run build",
    "build": "tsup src/index.ts -d bin --format esm --clean --env.NODE_ENV production",
    "dev": "tsup src/index.ts --watch -d bin --format esm --clean",
    "type-check": "tsc --noEmit",
    "format": "prettier --write --end-of-line=auto \"./**/*.{js,ts,json}\" --ignore-path .gitignore",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s"
  },
  "dependencies": {
    "chalk": "4.1.2",
    "find-up": "^6.2.0",
    "got": "12.0.0"
  },
  "devDependencies": {
    "conventional-changelog-cli": "2.2.2",
    "prettier": "2.5.1",
    "taze": "0.4.5",
    "tsup": "5.11.11",
    "typescript": "4.5.2",
    "vite": "2.7.10"
  },
  "peerDependencies": {
    "vite": "^2.0.0"
  },
  "engines": {
    "node": "^12.20.0 || ^14.13.1 || >=16.0.0"
  },
  "keywords": [
    "vite-plugin",
    "vite",
    "tauri"
  ]
}
